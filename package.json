{
  "name": "hello",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.js",
    "dev": "bun --watch run index.ts",
    "build": "bun run build-lib.ts && bun run build:declaration",
    "build:declaration": "tsc --emitDeclarationOnly --project tsconfig.types.json"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^15.7.3",
    "@types/bun": "^1.1.6",
    "@types/jest": "^29.5.12",
    "prettier": "^3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "diff-dom": "^5.1.4"
  }
}
